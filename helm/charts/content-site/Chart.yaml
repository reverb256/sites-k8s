apiVersion: v2
name: content-site
description: >-
  content.lan showcase + dashboard + approval API, served from the homelab k3s
  cluster. Replaces the nginx vhosts and the content-lan-approval service that
  the zephyr workstation hosted until 2026-09-23, under the standing directive
  "we do not want anything to be moved to zephyr".
type: application
version: 0.1.0
appVersion: "1.0.0"
